const UPDATE_POLLS = 'polls/UPDATE_POLLS'

const updatePolls = (data) => ({
  type: UPDATE_POLLS,
  data
})


const PollsReducer = (state = {}, action) => {
  let newState;
}

export default PollsReducer; 
